{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "mesh_db_count",
  "description": "Group-count audit-log entries by a single field.",
  "type": "object",
  "properties": {
    "group_by": {
      "type": "string",
      "enum": ["from_node", "to_surface", "decision", "type"]
    }
  },
  "required": ["group_by"],
  "additionalProperties": false
}
