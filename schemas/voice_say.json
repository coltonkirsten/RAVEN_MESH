{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "voice_say",
  "description": "Inject a text utterance for the model to speak verbatim through the voice session.",
  "type": "object",
  "properties": {
    "text": {"type": "string", "minLength": 1}
  },
  "required": ["text"],
  "additionalProperties": false
}
