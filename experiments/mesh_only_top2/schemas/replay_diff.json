{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "replay_diff",
  "description": "Compare the response payloads of two captured chains, position by position. Use to inspect what changed between an original run and a mutated replay.",
  "type": "object",
  "properties": {
    "left_correlation_id": {"type": "string", "minLength": 1},
    "right_correlation_id": {"type": "string", "minLength": 1}
  },
  "required": ["left_correlation_id", "right_correlation_id"],
  "additionalProperties": false
}
