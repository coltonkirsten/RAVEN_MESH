{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "composer_compose",
  "description": "Natural-language composition request: planner picks tools, calls them, returns the chain.",
  "type": "object",
  "properties": {
    "goal": {"type": "string", "minLength": 1, "description": "What the user wants done, in plain English."},
    "model": {"type": "string", "description": "OpenAI model id (default gpt-4o-mini)."},
    "max_steps": {"type": "integer", "minimum": 1, "maximum": 20, "description": "Cap on tool-call rounds (default 6)."},
    "dry_run": {"type": "boolean", "description": "If true, plan only — never invoke real surfaces."}
  },
  "required": ["goal"],
  "additionalProperties": true
}
