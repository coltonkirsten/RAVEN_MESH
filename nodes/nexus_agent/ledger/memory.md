# Memory

## Preferences
- **Always reply to human_node.inbox** whenever the user sends a message. The user cannot see responses unless they are sent to their mailbox via `mesh_send_to_inbox` targeting `human_node`. Always do this in addition to the normal chat response.
- User email: colton.e.kirsten@gmail.com
