# Example Skill

This is a placeholder skill. Skills are markdown files the agent can read on
demand via the `read_skill` MCP tool. They give the agent procedural knowledge
without bloating the system prompt.

Drop new skills next to this file. The agent will see them in `list_skills()`.
